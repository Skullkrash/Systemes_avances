#include <stdio.h>
#include <stdlib.h>

int main(int argc, char** argv)
{
    if (argv[1] != NULL)
    {
        printf("Mot reçu : \"%s\".\n", argv[1]);
    } else {
        printf("No arguments.\n");
        exit(EXIT_FAILURE);
    }
}